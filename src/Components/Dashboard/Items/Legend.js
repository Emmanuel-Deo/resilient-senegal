import React from "react";

export default function Legend() {
  return (
    <div style={{
      background: "#fff",
      padding: "6px 12px",
      borderRadius: "6px",
      fontSize: "14px",
      boxShadow: "0 2px 6px rgba(0,0,0,0.1)"
    }}>
      Legend
    </div>
  );
}
